# ORAM direct-call identity and equality-mask review

The retained diagnostic described in [the diagnostic retention ledger](oram-codegen-diagnostic-retention-2026-09-12.md) was statically reviewed without execution. Its ELF SHA-256 is `f5cc1c3fbd21b97d0e3882261813ab8632cb698c1111a73c285cddba892a398d`, from public GitHub run `34722711445`.

The review found exactly two defined text instantiations for each direct-call path used by fixed exact upsert: `Rng::random_range`, `CircuitORAM::read`, and `CircuitORAM::write_or_insert`. Both guarded upserts call the same RNG address. The checker now identifies these direct callees by legacy mangled path with a complete asserted count of two distinct identities at two distinct addresses. Existing fixed indirect identities, call order and multiplicity, instruction coverage, branch rules, and 38/82-byte selection-loop rules remain unchanged.

The review also demonstrated a prior blind spot by changing only the event equality mask's first byte in a scratch copy. The old checker still passed that changed ELF. The strengthened checker binds the event body's single 16-byte `%xmm7` mask to `ff` followed by fifteen zero bytes and derives and cross-checks its RIP-relative target. It rejects other non-call RIP-relative data accesses; the directory body permits no RIP-relative equality mask. Existing address-only `lea` instructions remain outside this data-load check, without a new pointer-dataflow claim.

Both checkers share the section parser, RIP target/comment parsing, read-only constant-span validation, and exact artifact-byte reader. The automatic provenance check requires one loaded, allocated, read-only data section containing the full constant, with no dynamic relocation starting in that section. It checks section flags, not load-segment permissions or arbitrary relocation write widths. Independently inspected program headers place all three actual mask addresses in the unique read-only `PT_LOAD` range `[0, 0x77f314)` with no writable overlap. This manual observation applies to the retained artifact; it is not a new automated program-header guard.

The original artifact passes both exact-upsert bodies (180 and 247 instructions) and all three unchanged page profiles (733 instructions each). The one-byte mask mutation fails with the specific changed-mask error. Independent validation passed 9 library, 53 upsert/access, and 21 page tests, plus strict Clippy and formatting. The original ELF digest remains unchanged. Checker commit `5770994e` contains the reviewed repair; fixed indirect identities, the separately pinned v0 identity, and all page profile files remain unchanged.

This is static review evidence for the guarded regions of the exact retained diagnostic. The artifact remains `qualified:false`, was not executed, and is not a release receipt or TDX/privacy qualification. A fresh native pipeline on the new checker source remains required.

## Second independent diagnostic

Public integration run `34724917398` completed with the old checker's exact-upsert identity rejection; the page check and checked packaging were skipped. Its retained artifact `10308220938` has ZIP SHA-256 `01985b4edf283aa1c827224739af349865bf4b547b8422f7431315d8391503ce` (67,451,784 bytes). All nine payload checksums in `SHA256SUMS` verify. The embedded receipt identifies merge commit `a13646e816e3ffa240bfdef608894de83cdefef3`, whose independently queried tree `f3a82d9fc7c8de01a8325d29ec7b550f36950a05` equals integration commit `3eb9d5a538d8740dc27972fee745e46f6201403c`'s tree.

The second ELF is 38,433,696 bytes, SHA-256 `56fab4e51e2e11f68d3df57a44795f94ec89dd33834d449271b71a62b4a89264`. Static inspection with the repaired checkers from `5770994e` and local GNU Binutils 2.47 passes both access-path instantiations, both exact-upsert bodies, and all three 733-instruction page transforms. The producer separately records GNU Binutils 2.42 and Rust 1.96.0. Neither diagnostic ELF was executed. The first artifact's manual program-header observations and mask-mutation experiment are not asserted for this second artifact.

This independently built diagnostic supports the identity repair without changing the failed run's historical conclusion or promoting its `qualified:false` receipt. Fresh native run `34726972640` at integration `8e4934d8b76c5667f9f9be1420e992bbcfc9ce24` remains the checked-packaging gate; at this verification checkpoint its Linux/macOS private-client and Linux confinement jobs passed, and its ORAM job was still running after strict Clippy passed.
